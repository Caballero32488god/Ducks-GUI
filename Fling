local ReplicatedStorage = game:GetService("ReplicatedStorage")
local flingEvent = ReplicatedStorage:WaitForChild("FlingPlayerEvent")

local function flingPlayer(player, targetPlayer)
    if targetPlayer and targetPlayer.Character then
        local humanoidRootPart = targetPlayer.Character:FindFirstChild("HumanoidRootPart")
        if humanoidRootPart then
            local bodyVelocity = Instance.new("BodyVelocity")
            bodyVelocity.Velocity = Vector3.new(0, 100, 0) -- Adjust direction and strength
            bodyVelocity.MaxForce = Vector3.new(10000, 10000, 10000)
            bodyVelocity.Parent = humanoidRootPart
            
            wait(0.5) -- Fling duration
            bodyVelocity:Destroy()
        end
    end
end

flingEvent.OnServerEvent:Connect(flingPlayer)
