Tabs.Main:AddButton({
    Title = "AntiKnockback",
    Callback = function()
        local Players = game:GetService("Players")
        local LocalPlayer = Players.LocalPlayer
        
        if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
            local humanoid = LocalPlayer.Character.Humanoid
            
            humanoid:GetPropertyChangedSignal("Health"):Connect(function()
                if humanoid.Health < humanoid.MaxHealth then
                    humanoid.Health = humanoid.MaxHealth
                end
            end)
            
            local function onKnockback(hit)
                if hit:IsA("BasePart") and hit.Parent == LocalPlayer.Character then
                    hit.Velocity = Vector3.new(0, 0, 0)
                end
            end
            
            LocalPlayer.Character.HumanoidRootPart.Touched:Connect(onKnockback)
            print("AntiKnockback activated.")
        else
            print("Character not found.")
        end
    end
})
