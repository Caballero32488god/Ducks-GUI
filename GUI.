local library = loadstring(game:HttpGet('https://raw.githubusercontent.com/jakepscripts/moonlib/main/moonlibv1.lua'))()
local window = library:CreateWindow("MoonLIB", "#b36419", 9160626035)

local Home = window:CreateTab("Home")
local Killer = window:CreateTab("Killer")
local AutoFarm = window:CreateTab("AutoFarm")
local PlayerStats = window:CreateTab("Player Stats")
local Teleport = window:CreateTab("Teleport")
local Settings = window:CreateTab("Settings")

Home:CreateLabel("Label", "Home")
Killer:CreateLabel("Label", "Killer")
AutoFarm:CreateLabel("Label", "AutoFarm")
PlayerStats:CreateLabel("Label", "Player Stats")
Teleport:CreateLabel("Label", "Teleport")
Settings:CreateLabel("Label", "Settings")

-- Toggles for specified tabs
Home:CreateToggle("Infinite Jump", "Enable infinite jumping", function(togglestate)
    -- // print(togglestate)
end)

Home:CreateToggle("FPS Unlocker", "Unlock FPS", function(togglestate)
    -- // print(togglestate)
end)

Killer:CreateToggle("Auto Kill", "Automatically kill enemies", function(togglestate)
    -- // print(togglestate)
end)

AutoFarm:CreateToggle("Auto Rebirth", "Automatically rebirth", function(togglestate)
    -- // print(togglestate)
end)

-- Display the GUI
library:Display()
